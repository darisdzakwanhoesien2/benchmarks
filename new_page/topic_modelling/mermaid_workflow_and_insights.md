# Mermaid Workflow and Insights (Topic Modelling Track)

Date: 2026-05-30

This note is generated by reading:

- Local generation outputs: `topic_modelling/output/*.md`
- Bibliography: `topic_modelling/scite.bib`
- Literature map export: `pages/litmaps.csv`
- Repo context (for workflow anchoring): `data/thesis_dataset/*/ocr_result.json`, `results/esg_records.json`, `topic_modelling/app.py`

---

## 1) End-to-End Research Workflow (Mermaid)

```mermaid
flowchart TD
  %% =========================
  %% Inputs
  %% =========================
  subgraph INPUTS[Inputs]
    A1[OCR corpus\n`data/thesis_dataset/*/ocr_result.json`]
    A2[ABSA-like records\n`results/esg_records.json`]
    A3[Lit references\n`topic_modelling/scite.bib` + `pages/litmaps.csv`]
    A4[Draft narrative sections\n`topic_modelling/output/*.md`]
  end

  %% =========================
  %% Corpus Profiling (already implemented)
  %% =========================
  subgraph PROFILING[Corpus Profiling / Readiness Diagnostics]
    B1[Full-corpus scan\n`topic_modelling/app.py`]
    B2[Coverage diagnostics\n(year, sector proxy)]
    B3[Pillar keyword signals\n(E/S/G proxy)]
    B4[Tone-vs-metric proxy\n(narrative-risk shortlist)]
  end

  %% =========================
  %% Corpus Builder (to implement)
  %% =========================
  subgraph CORPUS[Corpus Builder (Reproducible)]
    C1[Define unit of analysis\nstatement vs OCR-chunk]
    C2[OCR cleaning\nlayout artifacts, hyphenation, noise]
    C3[Bilingual normalization\nstopwords, language tags]
    C4[Metadata normalization\ncompany/year/sector/source]
    C5[Versioning\nmanifest + signature hash]
  end

  %% =========================
  %% Modeling
  %% =========================
  subgraph MODELING[Topic Modeling Experiments]
    D1[LDA baseline\nBoW topic-word + doc-topic]
    D2[NMF baseline\nTF-IDF factorization]
    D3[Embedding-based topics\nmultilingual embeddings + clustering]
    D4[Optional hybrids\nconstraints/priors + taxonomy cues]
  end

  %% =========================
  %% Evaluation
  %% =========================
  subgraph EVAL[Evaluation (Beyond Coherence)]
    E1[Intrinsic\ncoherence/diversity/overlap]
    E2[Stability\nmulti-seed + resampling]
    E3[Extrinsic alignment\nTopic→Pillar/Aspect/Tone/Sentiment]
    E4[Human interpretability\nkeywords + exemplars + labeling protocol]
  end

  %% =========================
  %% Outputs
  %% =========================
  subgraph EXPORTS[Standardized Exports]
    F1[`results/topic_modelling/`\ncorpus_manifest + preprocessing_config]
    F2[Topic tables\n`topics_<model>.csv`]
    F3[Unit-topic distributions\n`unit_topics_<model>.*`]
    F4[Alignment tables\nTopic×(pillar/aspect/tone)]
    F5[Metrics\ncoherence/stability summaries]
  end

  %% =========================
  %% Consumption
  %% =========================
  subgraph CONSUME[Downstream Use]
    G1[Streamlit exploration\n(read exported artifacts)]
    G2[Thesis evidence\nfigures/tables/discussion]
    G3[Audit workflows\nshortlists for manual review]
    G4[Cross-track integration\nSNA/summarization/fact-checking]
  end

  %% =========================
  %% Edges
  %% =========================
  A1 --> B1
  A2 --> C1
  A1 --> C1
  B1 --> B2
  B1 --> B3
  B1 --> B4

  B2 --> C4
  B3 --> C1
  B4 --> C1

  A3 --> H1
  A4 --> H2

  C1 --> C2 --> C3 --> C4 --> C5
  C5 --> D1
  C5 --> D2
  C5 --> D3
  D1 --> E1
  D2 --> E1
  D3 --> E1
  E1 --> E2 --> E3 --> E4

  E4 --> F1 --> F2 --> F3 --> F4 --> F5
  F5 --> G1
  F5 --> G2
  F5 --> G3
  F5 --> G4

  %% =========================
  %% Supporting: Narrative + Literature loops
  %% =========================
  subgraph SUPPORT[Supporting Loops]
    H1[Literature synthesis\n(robustness, bilingual topic models,\nESG disclosure mining)]
    H2[Paper drafting\n(section drafts → consolidated review)]
  end

  H1 -. informs .-> CORPUS
  H1 -. informs .-> MODELING
  H1 -. informs .-> EVAL
  H2 -. justifies .-> G2
  G2 -. requirements feedback .-> CORPUS
  G1 -. exploratory findings .-> EVAL
```

---

## 2) Literature-to-Workflow Mapping (Mermaid)

This diagram shows how the current literature artifacts feed specific engineering decisions.

```mermaid
flowchart LR
  subgraph LIT[Inputs: `scite.bib` + `litmaps.csv`]
    L1[OCR quality impacts\non downstream NLP]
    L2[OCR errors on topic modelling]
    L3[Embedding topic models\nrobustness to OCR noise]
    L4[Bilingual/cross-lingual\nprobabilistic topic models]
    L5[ESG disclosure mining\nand greenwashing studies]
    L6[General topic modelling\nsurveys/applications]
  end

  subgraph DESIGN[Design Decisions]
    D1[Unit of analysis\n(statement vs chunk vs section)]
    D2[Noise-aware preprocessing\n(OCR cleanup + boilerplate control)]
    D3[Model family selection\n(LDA/NMF vs embeddings vs hybrids)]
    D4[Evaluation design\n(stability + extrinsic alignment)]
    D5[Reproducibility artifacts\n(manifest + signatures + exports)]
  end

  L1 --> D2
  L2 --> D2
  L3 --> D3
  L4 --> D3
  L5 --> D4
  L6 --> D3
  L6 --> D4

  D1 --> D3
  D2 --> D3
  D3 --> D4
  D4 --> D5
```

---

## 3) Insights From `output/` (what exists now)

The `topic_modelling/output/` folder contains 10 markdown files that look like auto-generated draft sections for a review-style document:

- `output/001.md`: Abstract draft (contains noisy in-text citations formatting).
- `output/002.md`: Introduction draft, but it includes extra content not strictly matching “Intro only” and repeats keywords.
- `output/003.md`: Section 2 background draft.
- `output/004.md`–`output/008.md`: Sections 3–7 style content (methods, preprocessing, evaluation, best practices, repository-ready synthesis).
- `output/009.md`: “Research gaps / future directions” style section (7 items).
- `output/010.md`: A synthesis / conclusion-style section (labeled “Section 8” in the file, but functions like a conclusion).

**Key insight:** these `output/*.md` files are useful as raw drafting material, but they are not cleanly consistent in section numbering and citation formatting. For thesis/publication readiness, they should be treated as *notes* to be consolidated into a single controlled manuscript (e.g., `review_paper.md`), with:

- consistent section numbering,
- consistent reference keys,
- removal of malformed citations like `Strien et al., 2020; ,`.

---

## 4) Insights From `scite.bib` (what the current reference set emphasizes)

`topic_modelling/scite.bib` currently contains **26 BibTeX entries** (26 unique DOIs). The dominant themes are:

1. **OCR quality impacts on downstream NLP tasks**  
   - Multiple entries address how OCR errors affect NLP tasks broadly, including NER and downstream extraction.
2. **OCR errors specifically affecting topic modelling**  
   - Includes work explicitly about OCR error impact on topic modelling robustness.
3. **Robustness of embedding-based topic models under OCR noise**  
   - Supports the methodological justification for comparing LDA/NMF vs embedding-based topic models on OCR corpora.
4. **Bilingual / cross-lingual topic models**  
   - Includes crosslingual transfer in probabilistic topic models and bilingual topic modelling for comparable corpora.
5. **ESG/climate disclosure text mining**  
   - At least one entry directly targets climate-related disclosures and text mining.

**Actionable insight:** the bibliography is strong on OCR robustness and cross-lingual topic modelling foundations, which directly support Sections 4–5 of the review paper and the evaluation design. It is weaker on Indonesia-specific ESG disclosure mining and on “topic evaluation metrics” classics; those can be added later via a dedicated bibliography pass.

---

## 5) Insights From `litmaps.csv` (coverage and gaps)

`pages/litmaps.csv` contains **40 rows** (37 unique DOIs), all tagged `thesis`. The years observed are **2020–2025**.

Observed clusters in the list:

1. **OCR and long-context OCR pipelines** (e.g., DeepSeek-OCR type entries)  
   - Useful for discussing OCR pipeline evolution, but not always directly about ESG topic modelling.
2. **ESG disclosure and greenwashing frameworks**  
   - Includes work directly about climate talk–walk and greenwashing detection (some entries are broader than topic modelling).
3. **ESG keyword mining and “index building” using text**  
   - Supports “why textual themes matter” and motivates topic modelling outputs as structured signals.

**Actionable insight:** `litmaps.csv` broadens the context beyond topic modelling (OCR systems, greenwashing KG/RAG). This is helpful for framing “why” and “what next,” but should be curated so the core method review remains focused on topic modelling and evaluation.

---

## 6) Recommended Next Steps (to operationalize this workflow)

1. Consolidate the drafts:
   - Use `output/*.md` as inputs but clean them into `review_paper.md` (consistent sections + consistent citations).
2. Build a minimal “topic modelling exporter”:
   - Add `topic_modelling/run_topic_models.py` to generate `results/topic_modelling/` artifacts (manifest, topics, unit-topic distributions, alignment tables).
3. Add a bibliography hygiene pass:
   - Normalize BibTeX keys, remove malformed citations in drafts, and categorize refs by workflow stage (OCR, bilingual topic modelling, evaluation, ESG disclosure mining).

