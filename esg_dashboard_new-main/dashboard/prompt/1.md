You are creating an ESG aspect taxonomy for mapping noisy extracted aspect labels into stable reporting groups.

Return ONLY valid JSON. Do not include markdown, comments, explanations, or trailing commas.

Use exactly this schema:

{
  "reporting_framework_aspects": {
    "<Major_Group>": {
      "<sub_group>": [
        "<keyword_or_phrase>",
        "<keyword_or_phrase>"
      ]
    }
  }
}

Rules:
- The root key must be exactly "reporting_framework_aspects".
- Major groups should use Title_Case or Pascal_Case with underscores, for example:
  - Environmental
  - Social
  - Governance
  - Financial
  - Operations_and_Strategy
- Subgroups must use lowercase snake_case, for example:
  - climate_and_energy
  - resource_and_waste
  - labor_and_employees
  - stakeholder_relations
- Keywords must be short lowercase phrases unless they are official acronyms.
- Include synonyms and common variants from noisy extracted aspect labels.
- Do not duplicate the same keyword across multiple subgroups unless absolutely necessary.
- Prefer specific phrases over overly broad one-word labels.
- If a phrase could belong to multiple groups, place it in the most likely reporting context.
- Include an "Other" major group only if the terms are truly unmappable.

Create a taxonomy for the following unmapped aspect values:

<PASTE_UNMAPPED_ASPECT_VALUES_HERE>
x