Given these unmapped ESG aspect values, create or extend a taxonomy that maps them into stable reporting groups.

Return ONLY valid JSON using this exact schema:

{
  "reporting_framework_aspects": {
    "Environmental": {},
    "Social": {},
    "Governance": {},
    "Financial": {},
    "Operations_and_Strategy": {}
  }
}

For each major group, create lowercase snake_case subgroups and place matched aspect phrases as keyword arrays.

Important:
- Every input aspect should appear exactly once as a keyword, unless it is empty or meaningless.
- Preserve the original wording of each aspect value as a keyword.
- Add close synonyms only when useful for future matching.
- Use valid JSON only.
- No markdown.
- No explanation.

Unmapped aspect values: